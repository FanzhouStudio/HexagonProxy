# HexagonProxy v0.2.0

本次更新重点修复 Codex 多账号授权和启动流程。已通过实际使用验证，可以在 Codex 桌面端运行期间打开浏览器授权并添加账号。

## 修复与改进

- **修复授权添加账号立即失败**：优先查找用户目录中的官方 Codex CLI，并检查程序是否可运行，避免误用 WindowsApps 目录中无法执行的程序。
- **修复浏览器授权启动失败**：修正进程启动参数冲突，识别官方授权链接并调用默认浏览器。
- **修复一直显示“Codex 启动中”**：助手通过独立结果文件返回状态，避免等待桌面子进程关闭；增加超时处理，防止操作长期卡住。
- **支持运行中添加授权账号**：在独立目录完成登录，无需先退出 Codex；账号切换仍会重启 Codex 桌面端。
- **改进错误提示**：异常时显示错误类型和代码位置，便于定位问题，不输出登录令牌。
- 保留独立账号凭证库、账号去重、额度自动刷新、恢复时间显示，以及切换失败恢复机制。

## 使用说明

1. 彻底退出旧版 HexagonProxy，再用新版 `HexagonProxy.exe` 覆盖旧文件。
2. 打开“Codex 账号”，点击“授权添加账号”，在浏览器完成登录。
3. 授权保存后，从账号列表切换。切换会重启 Codex，请先结束其中正在运行的任务。

已有账号配置会继续保留。若授权已被服务端撤销，仍需重新授权。

## 验证

- 已通过自动化测试，包括账号服务、凭证助手、额度刷新和界面检查。
- 已在不包含 Codex CLI 的 PATH 环境下验证程序发现和浏览器授权启动。
- 已经用户实际使用验证可正常使用。

Windows 便携版：`HexagonProxy.exe`。校验值见随包提供的 `SHA256SUMS.txt`。
